[package]
name = "rsb-libraw"
version = "0.0.1"
edition.workspace = true
publish.workspace = true

[dependencies]
rsb-libraw-ffi = { workspace = true }
jpeg-decoder = { version = "0.3", default-features = false }

